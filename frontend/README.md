# ADAPT frontend

Phase 10 Next.js learner product. It does not implement adaptive decisions.

```bash
# terminal 1
python -m app

# terminal 2
npm install
npm run dev
```

Open http://127.0.0.1:3000. API requests to `/api` are rewritten to the Python server on port 8765.
