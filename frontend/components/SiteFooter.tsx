export function SiteFooter() {
  return (
    <footer className="border-t border-line bg-paper px-4 py-8 text-sm text-muted sm:px-6">
      <div className="mx-auto flex max-w-6xl flex-col gap-2 sm:flex-row sm:items-center sm:justify-between">
        <p>ADAPT demonstrates evidence-sensitive adaptive tutoring. Educational efficacy is inconclusive (n = 0).</p>
        <p>Offline. No API key. Engine remains authoritative.</p>
      </div>
    </footer>
  );
}
